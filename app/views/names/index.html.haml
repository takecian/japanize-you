%h1 Listing names
.center
  = @names.total_count
  件中
  = @names.offset_value
  〜
  = @names.offset_value + @names.length
  件を表示しています

.center
  = paginate @names, window: 3, outer_window: 2

%table.table
  %tr
    %th Name
    %th Hiragana
    %th Katakana
    %th
    %th
    %th

  - @names.each do |name|
    %tr
      %td= name.name
      %td= name.hiragana
      %td= name.katakana
      %td= link_to 'Show', name
      %td= link_to 'Edit', edit_name_path(name)
      %td= link_to 'Destroy', name, :method => :delete, :data => { :confirm => 'Are you sure?' }

%br

= link_to 'New Name', new_name_path
